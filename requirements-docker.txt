# Production ML Requirements (Google Cloud Platform)
# This file contains ALL ML libraries for production deployment on GCP Cloud Run
# For Vercel staging, use backend/requirements.txt instead

# Core FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gunicorn>=21.0.0  # Production WSGI server
python-multipart>=0.0.9
starlette>=0.49.1

# Database and Caching
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg2-binary>=2.9.9  # PostgreSQL driver for Cloud SQL
redis>=5.0.1
aioredis>=2.0.1

# Pydantic and settings
pydantic[email]>=2.10.0
pydantic-settings>=2.6.0
pyyaml>=6.0

# Authentication and Security
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt==4.2.1  # bcrypt 4.2.x has better passlib compatibility
webauthn>=2.5.1
pyotp>=2.9.0
cbor2>=5.8.0

# API clients and data
httpx>=0.28.0
aiohttp>=3.9.0
websockets>=12.0
python-dotenv>=1.0.1
requests>=2.32.4
urllib3>=2.6.0

# Background Tasks
celery>=5.3.4

# Data Processing & Analytics
pandas>=2.2.0
numpy>=1.26.4
scipy>=1.14.0

# ML Libraries (Production)
# Note: tensorflow, torch, transformers removed - not used in backend, causes build failures
scikit-learn>=1.5.0
xgboost>=2.1.0

# Financial & Trading Libraries
yfinance>=0.2.48
alpha-vantage>=2.3.1
# alpaca-trade-api removed - broker uses direct HTTP calls
# ta-lib removed - requires system libta-lib not in container
# quantlib removed - requires system Boost libraries not in container

# Visualization
plotly>=5.17.0
matplotlib>=3.8.2

# Logging
structlog>=24.4.0

# Security: Pin these to fix known vulnerabilities
pillow>=11.3.0

# Note: This is for GCP Cloud Run production deployment
# Cloud Run has no size limits like Vercel serverless functions
