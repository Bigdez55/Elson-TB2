# CI/CD Requirements - Lightweight for GitHub Actions
# Does NOT include heavy ML packages (tensorflow, torch, qiskit)
# Those are only needed in production and would exhaust CI disk space

# Core FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
starlette>=0.49.1

# Database
sqlalchemy>=2.0.36
alembic>=1.14.0

# Pydantic and settings
pydantic[email]>=2.10.0
pydantic-settings>=2.6.0

# Authentication and Security
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1.0
webauthn>=2.0.0
pyotp>=2.9.0
cryptography>=43.0.0

# API clients and data
httpx>=0.28.0
python-dotenv>=1.0.1
requests>=2.32.4
urllib3>=2.6.0

# Logging
structlog>=24.4.0

# Data Processing (lightweight)
pandas>=2.2.0
numpy>=1.26.4

# ML Libraries (lightweight only - no tensorflow/torch/qiskit)
scikit-learn>=1.5.0
joblib>=1.3.0

# Financial & Trading Libraries
yfinance>=0.2.48

# Security
pillow>=11.3.0
aiohttp>=3.9.0

# Redis and Caching
redis>=5.0.0

# WebSocket support
websockets>=12.0

# Local packages
pyyaml>=6.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
