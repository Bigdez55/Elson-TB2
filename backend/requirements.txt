# Core FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
starlette>=0.49.1

# Database
sqlalchemy>=2.0.36
alembic>=1.14.0

# Pydantic and settings
pydantic[email]>=2.10.0
pydantic-settings>=2.6.0

# Authentication and Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0

# API clients and data
httpx>=0.28.0
python-dotenv>=1.0.1
requests>=2.32.4
urllib3>=2.6.0

# Logging
structlog>=24.4.0

# Data Processing
pandas>=2.2.0
numpy>=1.26.4
scipy>=1.14.0

# ML Libraries (ACTUALLY USED in your backend)
scikit-learn>=1.5.0
xgboost>=2.1.0

# Financial & Trading Libraries
yfinance>=0.2.48
alpaca-trade-api>=3.2.0

# Security: Pin these to fix known vulnerabilities
pillow>=11.3.0
aiohttp>=3.12.14

# REMOVED (not used in backend, too large for Vercel):
# - tensorflow (2GB+)
# - torch (1GB+)
# - transformers (500MB+)
# These would cause Vercel deployment to fail due to size limits
