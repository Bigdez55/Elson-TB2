# Core FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.12.1

# Pydantic and settings
pydantic[email]==2.5.3
pydantic-settings==2.1.0

# Authentication and Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# API clients and data
httpx==0.25.2
python-dotenv==1.0.0
requests==2.31.0

# Logging
structlog==23.2.0

# Data Processing (lightweight)
pandas==2.1.4
numpy==1.26.2

# Financial & Trading Libraries (essential only)
yfinance==0.2.28
alpaca-trade-api==3.1.1

# Note: Heavy ML libraries (TensorFlow, PyTorch) removed for Vercel deployment
# If you need ML features, consider deploying to Railway, Render, or Google Cloud Run
