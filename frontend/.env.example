# API Configuration
# For local development
# REACT_APP_API_URL=http://localhost:8000/api/v1

# For production (Vercel deployment)
# The API URL will default to /api/v1 which works with the proxy configuration
# If you're deploying to a different backend, set this to the full backend URL
REACT_APP_API_URL=/api/v1
